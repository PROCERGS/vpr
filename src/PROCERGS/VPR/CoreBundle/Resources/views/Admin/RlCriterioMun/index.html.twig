{% extends '::base.html.twig' %}

{% block body -%}
    <h1>RlCriterioMun list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Perccitizen</th>
                <th>Percvoter</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('rlcriteriomun_show', { 'id': entity.id }) }}">{{ entity.percCitizen }}</a></td>
                <td>{{ entity.percVoter }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('rlcriteriomun_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('rlcriteriomun_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('rlcriteriomun_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
