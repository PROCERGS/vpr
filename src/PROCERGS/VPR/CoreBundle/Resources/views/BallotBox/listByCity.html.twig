{% extends 'PROCERGSVPRCoreBundle::base.html.twig' %}

{% block body %}

<div class="row">
  <div class="col-md-12 text-left">
    <h2 class="main-title place-search-title">
      <span class="icon"></span>
      <span>{{ 'Polling Places' | trans }}</span>
    </h2>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <div class="inner-content">
      <p>{% trans %}Type a city to see it's presential polling places.{% endtrans %}</p>
      {{ form_start(form, {'attr' : {'role' : 'form', 'class' : 'form-inline'}}) }} {{ form_errors(form) }}
      <div class="form-group">
          {{ form_label(form.city) }}
      </div>
      <div class="form-group">
          {{ form_errors(form.city) }}
          {{ form_widget(form.city, {'attr': {'class': 'form-control city'}}) }}
      </div>
      <div class="form-group">
        {{ form_widget(form.submit, {'attr': {'class': 'btn btn-success'},'label':'Search' | trans}) }}
      </div>
    </div>
    {{ form_end(form) }}
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    {% if boxes %}
    <div class="inner-content">
      <h3 class="subtitle">{% trans %}Polling places in your city.{% endtrans %}</h3>
      <table class="table table-striped">
        <thead>
          <tr>
            <th>Votação</th>
            <th>Endereço</th>
          </tr>
        </thead>
        <tbody>
          {% for box in boxes %}
          <tr>
            <td>{{ box.poll.name }}</td>
            <td>{{ box.address }}</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
    {% endif %}
  </div>
</div>

{% endblock %}

{% block stylesheets %}
{{ parent() }}
{% stylesheets 'bundles/procergsvprcore/css/search/search.css' filter='cssrewrite' %}
<link rel="stylesheet" href="{{ asset_url }}" />
{% endstylesheets %}
{% endblock %}

{% block javascripts %}
{{ parent() }}
<script>
  var citiesList = {{ cities | raw }};
</script>
{% javascripts
  '@PROCERGSVPRCoreBundle/Resources/public/js/components/citiesTypeahead.js' %}
  <script type="text/javascript" src="{{ asset_url }}"></script>
  {% endjavascripts %}
  {% endblock %}
